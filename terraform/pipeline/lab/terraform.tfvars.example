# Copy to terraform.tfvars only when the defaults are not right. Every value has a default in variables.tf.
# ops/up.sh passes owner with -var. terraform/base/core must be applied first (this root reads its state file).
#
# region      = "ap-northeast-1"
# name_prefix = "netops-poc"
# owner       = "netops"
#
# instance_type     = "t4g.large"
# ami_ssm_parameter = "/aws/service/ami-amazon-linux-latest/al2023-ami-kernel-default-arm64"
# volume_size       = 16
# auto_start_lab    = true
#
# containerlab_version = "0.79.0"
# telegraf_version     = "1.40.0"
# frr_image_tag        = "10.2.1"
# snmpd_image_tag      = "v1"
# multitool_image_tag  = "v0.10.0"
